import { Options, RequestOptions } from "./types/options";
declare class Api {
    private axios;
    config: {
        core: {
            sandbox: string;
            production: string;
        };
        snap: {
            sandbox: string;
            production: string;
        };
        iris: {
            sandbox: string;
            production: string;
        };
    };
    isProduction: boolean;
    serverKey: string;
    constructor({ serverKey, isProduction }: Options);
    /**
     * Get Statis Snap Url
     * @returns String
     */
    get snapUrl(): string;
    /**
     * Get Statis Core Api Url
     * @returns String
     */
    get coreUrl(): string;
    /**
     * Get Statis Iris Url
     * @returns String
     */
    get irisUrl(): string;
    get({ url, data, headers }: RequestOptions): Promise<any>;
    post({ url, data, headers }: RequestOptions): Promise<any>;
    patch({ url, data, headers }: RequestOptions): Promise<any>;
}
export default Api;
