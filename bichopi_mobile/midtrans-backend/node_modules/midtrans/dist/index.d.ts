import Api from "./api";
import CoreApi from "./method/coreApi";
import SnapApi from "./method/snap";
import { Options } from "./types/options";
import { Transaction } from "./types/transaction";
declare class Midtrans {
    api: Api;
    core: CoreApi;
    snap: SnapApi;
    constructor(options: Options);
    /**
     *
     * @param transactionId
     * @returns
     */
    status(transactionId: string): Promise<any>;
    /**
     *
     * @param transactionId
     * @returns any
     */
    statusb2b(transactionId: string): Promise<any>;
    approve(transactionId: string): Promise<any>;
    deny(transactionId: string): Promise<any>;
    cancel(transactionId: string): Promise<any>;
    expire(transactionId: string): Promise<any>;
    refund(transactionId: string, parameter: Record<string, any>): Promise<any>;
    refundDirect(transactionId: string, parameter: Record<string, any>): Promise<any>;
    notification(transactionId: string): Promise<any>;
}
export { Midtrans, Options, Transaction };
